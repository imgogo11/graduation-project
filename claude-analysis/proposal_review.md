# 开题报告修改记录（03.docx → 04.docx）

## 修改汇总

共修改 **8 处**，分为两类：**名称统一** 和 **内容优化**。

---

### 一、名称统一（6 处）

将全文所有 **"融合C++算法引擎"** 替换为 **"基于C++算法模块"**，保持与新确定的课题名称一致。

| 位置 | 原文片段 | 修改后 |
|------|---------|--------|
| 段落[39] 选题背景 | 一套"融合C++算法引擎的交易数据管理与分析系统" | 一套"基于C++算法模块的交易数据管理与分析系统" |
| 段落[42] 选题背景 | C++算法引擎与Web系统统一到同一业务链路 | C++算法模块与Web系统统一到同一业务链路 |
| 段落[66] 研究方法 | 项目采用C++23实现核心算法模块 | *(此处原文已用"模块"，无需改)* |
| 段落[73] 预期结果 | "融合C++算法引擎的交易数据管理与分析系统" | "基于C++算法模块的交易数据管理与分析系统" |
| 进度表 Row 4 | C++算法引擎接入 | C++算法模块接入 |

---

### 二、内容优化（2 处）

#### 1. 删除不当自我指涉（段落[64]）

```diff
- 在工程架构上，本项目并未采用文中原先所写的"冷热分离架构与分块缓冲机制"，而是采用"数据持久化层、分析服务层、算法索引层"分层组织的思路。
+ 在工程架构上，本项目采用"数据持久化层、分析服务层、算法索引层"分层组织的思路。
```

> [!IMPORTANT]
> 开题报告中不应出现"文中原先所写的"这种自我指涉的表述。正式学术文档应该直接陈述所采用的方案。

#### 2. 明确算法名称（段落[53]、[62]）

```diff
- 基于可持久化思路的精确区间第K大成交量查询
+ 基于可持久化线段树的精确区间第K大成交量查询
```

```diff
- 系统按交易日期顺序构建版本化结构
+ 系统按交易日期顺序构建可持久化线段树
```

```diff
- 系统可基于线段树完成索引构建与查询
+ 系统基于线段树完成索引构建与查询
```

> [!NOTE]
> "可持久化思路"和"版本化结构"过于模糊，你的代码实际实现就是经典的**可持久化线段树（主席树）**，直接写出来更准确、更有说服力。

---

## 未修改但值得注意的问题

以下是我在审阅中发现的但**未在本次修改中处理**的内容（因为涉及较大改动，需要你确认）：

1. **参考文献编号不一致**：文献综述中引用 `[1]-[14]`，开题报告中引用 `[1]-[13]`，但两部分的 `[1]` 分别指向不同论文（文献综述的 `[1]` 是时序数据库综述，开题报告的 `[1]` 是常城的学位论文）。建议确认你的学校格式是否允许两部分分别编号
2. **文献综述缺少对 CDQ 分治的讨论**：项目实际使用了 CDQ 分治做联合异常排序，但文献综述中未提及相关算法文献
3. **进度表日期已过半**：当前日期 2026-04-12 已经进入最后一阶段（2026.4.12～2026.5.9），进度表内容可能需要与实际进展同步

---

## 格式说明

所有修改仅涉及**正文文字内容**，未修改任何格式（字体、字号、段落样式、页眉页脚等）。
